rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for development - this bypasses permission issues
    match /{document=**} {
      allow read, write, create, update, delete: if true;
    }
  }
}
